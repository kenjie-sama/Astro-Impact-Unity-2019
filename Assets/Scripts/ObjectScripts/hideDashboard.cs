using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hideDashboard : MonoBehaviour
{
    /*[SerializeField] private GameObject dashboardNext;
    public void runDashboardTransition()
    {
        if (dashboardNext != null)
        {
            Animator animateDashboard = dashboardNext.GetComponent<Animator>();
            if (animateDashboard!=null)
            {
                bool hasDashboardNext = animateDashboard.GetBool("isNext");
                animateDashboard.SetBool("isNext",!(hasDashboardNext));
            }
        }
    }*/
}
