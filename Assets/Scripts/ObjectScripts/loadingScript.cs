using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class loadingScript : MonoBehaviour
{
    /*[SerializeField] private GameObject loadingText;
    private Animator textAnimations;
    private int currentNumber;
    void Awake()
    {
        textAnimations = loadingText.GetComponent<Animator>();
        currentNumber = 0;
    }
    void Update()
    {
        currentNumber = 0;
        currentNumber = Random.Range(0, 2);
        if (currentNumber==0) {
            currentNumber = Random.Range(0, 2);
            textAnimations.SetInteger("loadingMessageNumber",currentNumber);
        }
        if (currentNumber == 1)
        {
            currentNumber = Random.Range(0, 2);
            textAnimations.SetInteger("loadingMessageNumber", currentNumber);
        }
        if (currentNumber == 2)
        {
            currentNumber = Random.Range(0, 2);
            textAnimations.SetInteger("loadingMessageNumber", currentNumber);
        }
    }*/
}
